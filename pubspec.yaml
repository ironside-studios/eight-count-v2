name: eight_count
description: "8 Count V2 — Boxing timer. Every Round Counts."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.5
  go_router: ^16.2.4
  google_fonts: ^6.3.3
  intl: ^0.20.2
  google_mobile_ads: ^6.0.0
  lucide_icons_flutter: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
